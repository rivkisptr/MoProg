import 'package:flutter/material.dart';

class HW extends StatelessWidget {
  const HW({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("ALAMAKK"),
      ),
      body: const Center(
        child: Text("HW"),
      ),
    );
  }
}
